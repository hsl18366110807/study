协程的实现
    协程是用户态操作一个线程，实现并发处理的过程。
    本质是用户态的任务调度。
    任何时间都只有一个协程在跑。
    通过yield和resume来对任务进行并发处理
    自带cpu上下文，切换协程就是保存和恢复cpu上下文即可。
    在高io密集的程序程序里效果比较好。
简历中jps，mesh算法
数据库表对象的底层的实现
utf-8和gdk的区别
    utf-8是国际通用的编码方式，不区分英文和中文。
    英文1个字节，中文2-3字节
    中文的话n个字节，第一个字节的前n位为1，后面几个字节的开头10
    gdk是任何字符不管中文英文都是两个字节编码。gdk是gd2313的扩展。

